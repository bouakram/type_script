// interfaces 

interface user {
    name : string, 
    age : number,
    country : string
}

let user : user = {
    name : "akram",
    age : 25,
    country : "algeria"
}

let user1 : user = {
    name : "aymen",
    age : 27,
    country : "algeria"
}

console.log(user)
console.log(user1)